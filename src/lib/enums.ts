export enum Plan {
  BASIC = "basic",
  PLUS = "plus",
  ULTRA = "ultra",
}

export enum UserStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
}

export enum RoadmapStatus {
  PENDING = "pending",
  DONE = "done",
}
